# baseURL DEBE ser solo el texto, sin corchetes ni paréntesis
baseURL = "https://danielrn13.github.io/monalisa-snacks.github.io/"
languageCode = "es-mx"
title = "Monalisa Crepes & Burgers"
theme = "ananke"

# Forzamos rutas absolutas para evitar confusiones en subcarpetas
canonifyURLs = true
relativeURLs = false

[params]
  author = "Mona"
  description = "Las mejores crepas y hamburguesas artesanales."
  # Ruta absoluta con el nombre del repositorio
  featured_image = "/monalisa-snacks.github.io/images/banner-principal.jpg"

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true

# ELIMINAMOS COMPLETAMENTE LA SECCIÓN [[menu.main]] PARA QUITAR LA PALABRA "INICIO"